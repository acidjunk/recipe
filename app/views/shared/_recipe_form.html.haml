%h3 Nieuw recept
= form_for(@recipe) do |f|
  = render 'shared/error_messages', object: f.object
  =f.label :name
  =f.text_field :name
  .field
    =f.text_area :content, placeholder: 'Tik hier je recept...'
  =f.submit('Plaats recept', class:'btn btn-large btn-primary')

